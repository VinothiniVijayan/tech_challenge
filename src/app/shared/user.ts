export interface User {
    uid: string;
   email: string;
   displayName: string;
   photoURL: string;
   emailVerified: boolean;
 }

 export interface UserDetails{
    email: string;
    firstName: string;
    lastName: string;
    mobileNo: number;
    address: string;
 }